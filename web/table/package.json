{
  "name": "table-tableform",
  "title": "Table & TableForm",
  "description": "Table if for creating table with data from server or localy, posibility to order, filter by column and some actions like add, edit, delete.\nTableForm is for generating form, is like row add, edit. Is only for internal Table use",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^0.12.7"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-autoprefixer": "^3.0.1",
    "gulp-coffee": "^2.3.1",
    "gulp-concat": "^2.6.0",
    "gulp-rename": "^1.2.2",
    "gulp-ruby-sass": "^2.0.5",
    "gulp-uglify": "^1.4.2",
    "gulp-util": "^3.0.7",
    "gulp-sourcemaps": "^1.6.0"
  }
}
