   <script>
  $(function() {
      
    var availableTags = [
  {% if data.search1 is defined %}
        {% for search in data.search1 %}
      "{{search.Nm}}",
      {% endfor %}{%endif%}
      " "
    ];
    $( ".tags" ).autocomplete({
      source: availableTags
    });
  });
  
  </script>
